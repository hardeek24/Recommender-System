model:
  embedding_dim: 512
  hidden_dim: 1024
  num_layers: 3
  dropout: 0.1
  
two_tower:
  user_tower_dim: 256
  item_tower_dim: 256
  
llm_judge:
  model_name: "gpt-4"
  max_tokens: 1000
  temperature: 0.7

training:
  batch_size: 32
  learning_rate: 0.001
  epochs: 100